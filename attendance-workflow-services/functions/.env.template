# This is a template for environment variables.
# Create a .env file in this directory for local development with the emulators.
# DO NOT commit the .env file to version control.

# The Google Cloud project ID to use for the emulators.
GCP_PROJECT=attendance-app-dev
FUNCTION_TARGET=

# NOTE: Production secrets (e.g., API keys for SendGrid, Google Maps)
# are NOT configured here. They are managed securely using Google Secret Manager
# as per REQ-1-069 and accessed at runtime by the Cloud Functions.
# The functions/src/config.ts file should contain logic to fetch these secrets.
# Example secrets that would be in Secret Manager:
# SENDGRID_API_KEY=
# GOOGLE_MAPS_API_KEY=

LOG_LEVEL=info