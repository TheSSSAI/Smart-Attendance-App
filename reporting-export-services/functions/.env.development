# Development environment variables
# These are loaded by the Firebase Emulator Suite using `dotenv`

# Google OAuth Client ID for the data export feature
GOOGLE_CLIENT_ID="your-dev-google-client-id.apps.googleusercontent.com"

# The name of the secret in Google Secret Manager that holds the OAuth Client Secret
GOOGLE_CLIENT_SECRET_NAME="google-oauth-client-secret-dev"

# The project ID where secrets are stored
GCP_PROJECT_ID="attendance-app-dev"