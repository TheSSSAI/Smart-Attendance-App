# This file is for LOCAL DEVELOPMENT with the Firebase Emulator Suite ONLY.
# In deployed environments (dev, staging, prod), secrets are loaded from Google Secret Manager.

# SendGrid API Key for local testing (can be a test key)
# In production, this value is loaded from Secret Manager and should NOT be set here.
SENDGRID_API_KEY="YOUR_LOCAL_OR_DEV_SENDGRID_API_KEY"

# Firebase Emulator Configuration
FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"
FIREBASE_AUTH_EMULATOR_HOST="127.0.0.1:9099"
PUBSUB_EMULATOR_HOST="127.0.0.1:8085"

# This tells the Firebase Admin SDK that we are running in an emulated environment.
# Do not set this in deployed environments.
FIREBASE_STORAGE_EMULATOR_HOST="127.0.0.1:9199"