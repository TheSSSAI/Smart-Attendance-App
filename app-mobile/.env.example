# This file is an example. Create a .env file in the root directory
# and add your environment-specific variables.
# DO NOT COMMIT the .env file to version control.

# Google Maps API Keys (REQ-1-014)
# Get these from your Google Cloud Platform project console.
# Ensure keys are restricted to your app's bundle ID (iOS) and package name/SHA-1 (Android).
GOOGLE_MAPS_API_KEY_IOS=YOUR_IOS_GOOGLE_MAPS_API_KEY_HERE
GOOGLE_MAPS_API_KEY_ANDROID=YOUR_ANDROID_GOOGLE_MAPS_API_KEY_HERE

# Note: Firebase configurations are handled via firebase_options.dart,
# which is environment-specific and generated by the FlutterFire CLI.
# No Firebase keys should be stored here.